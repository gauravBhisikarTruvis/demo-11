All SQL queries must be valid BigQuery syntax.

CRITICAL: Every table reference in the final SQL code must be fully qualified with its dataset name (e.g., use analytics.events instead of just events). Do not generate SQL with bare table names.
